; PHP per-directory overrides (commonly honored by PHP-FPM / CGI).
; Note: requires PHP setting `user_ini.filename` to be enabled (default: .user.ini).

upload_max_filesize=10M
post_max_size=12M
memory_limit=512M
max_execution_time=120
max_input_time=120

